# Discord bot token (required)
DISCORD_TOKEN=

# OpenAI (optional, needed for /ai)
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# Optional: faster guild-only slash command sync during development
# SYNC_GUILD_ID=
